spring:
  datasource:
    driver-class-name:
    url : jdbc:mysql://localhost:3306/ddl_induction
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

